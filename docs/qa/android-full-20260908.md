# Android 全功能验收台账（2026-09-08）

基线：fd2ec51；独立分支 test/android-full-20260908。设备为 NightFlix_API35 / Android 15 模拟器。下方首轮结果保留历史；用户随后明确授权完整测试、修复上线并更新固定二维码，最新处理见文末。

## 已完成

| 范围 | 实际验证 | 证据 |
| --- | --- | --- |
| 设置、账户 | 9 个业务场景通过：播放设置与持久化、15 语言、注册/错误验证码/协议、通知失败回滚、设备撤销、修改/重置密码、分页导出/反馈/清缓存、原生设置与分享取消、注销后拒绝登录 | settings-run.log；settings-screenshots/ |
| 登录后页面 | 金币空账本、会员有效/过期筛选、商店关闭提示、消息详情与数据库已读、已读消息再次打开、奖励钱包、片库空态与下拉刷新；1 个补充用例通过 | nightflix-full-native-final-20260908.log |
| 原生播放器/OCR | 暂停/恢复、第 10 集、2 倍速、拖动进度、关闭自动切集、原生本机 OCR、中文设置与版本/权限信息；3 个用例通过 | 同上；视频为本地 fixture，不作为远程播放证据 |
| Flutter 单元/组件 | 64 项通过 | nightflix-full-unit-20260908.log；不能与模拟器按钮验收混算 |
| 真实 PostgreSQL | 排序权重、新剧排序、并发与下架、关注/收藏隔离、访客热度、反馈及回复、私有轨道行级权限，共 7 项通过 | nightflix-full-discovery-20260908.log；不是 App 操作链路 |
| 静态检查 | flutter analyze 无问题 | nightflix-full-analyze-final-20260908.log |

设置测试使用 127.0.0.1:4326 专用 Nest + PostgreSQL 临时数据库及非 owner 运行角色；只截获 example.test 邮件，不发外部邮件。测试结束调用 finish 清理本轮库和角色。测试消息只属于该临时库。登录操作正常产生测试会话。

## 远程导航补充验收

- 远程目录导航补充用例最终完整通过（1 分 24 秒）：真实远程视频暂停/恢复、第 10 集、点赞/收藏/追剧游客登录门槛、评论面板、Following 空态返回、排行榜/新剧/筛选、收藏游客门槛、无结果搜索/清空、识剧进入返回、奖励/片库/个人入口与语言弹窗关闭。日志 nightflix-full-catalog-final3-20260908.log。
- 首轮脚本错误分别是消息摘要与正文匹配重复、历史标签定位错误；已用详情组件/Tab 定位修正并重跑账户用例通过。
- 远程播放器脚本修正了等待播放状态、当前播放器实例与过渡空组件检测，并避免对已经可点击的按钮调用 ensureVisible 影响 PageView。登录门槛以提交按钮识别，不能假定带 reason 的弹窗也叫 Welcome back。首次失败结果保留，没有修改产品逻辑。

## 不具备条件或仍需补测

- 测试服务器 bootstrap：AdMob、Google 登录、Apple 登录、IAP 和原生回执验证均 false，storeProducts 为 0。只能验关闭态，不能验真实广告解锁、购买/恢复/续费/退款。
- 已登录点赞/收藏/追剧/评论创建、删除、举报、分页，以及付费剧金币/整剧购买，仍需完整模拟器链路；已有组件/API 测试不替代。
- 字幕/配音真实轨道切换、原生截图/相机取消及权限拒绝到搜索完整链路、剧目分享/Deep Link、推送送达、离线/恢复网络/短时凭证过期与前后台切换，仍需补测。
- iOS 不在当前 Android 模拟器结果范围内。

## 源码发现（与实测失败区分）

- account_features.dart 的账户记录详情直接遍历 item.entries 显示技术字段；此次临时钱包没有流水，因此尚未通过模拟器截图验收这类有数据页面。
- 当前播放器未找到用户可选清晰度入口；不能声称原计划清晰度切换已实现并通过。

## 已确认问题 NF-ANDROID-01

从深色播放页打开白色登录弹窗，标题继承了深色页面的白色默认文字。实际 Android RichText 颜色为 RGB(1,1,1)，亮度 1.0，与白色背景无对比度。截图 catalog-current2.png 与 account_contrast_test.dart 复现；不是接口失败。AccountSheet 内部虽然设置浅色 Theme，但没有替换上层 Material 的 DefaultTextStyle，标题 Text 未显式设置颜色。此轮只测试，未修复业务实现。修复验收应同时覆盖播放器、剧场、个人中心打开登录/注册/重置弹窗，并检查标题及校验文案对比度。

证据目录：/Users/yewei/Documents/Codex/2026-08-31/zhe/outputs/android-full-20260908。
整体状态：尚未达到“所有按钮及所有状态全部验收通过”。

最终登录标题回归在 Android 上明确失败：期望亮度 < 0.5，实际 1.0；日志 nightflix-full-contrast-final-20260908.log。这个失败被保留，不改断言掩盖问题。
测试结束已重新安装普通体验包 verified-public-new.apk（1.0.1 / 2026090707）并冷启动；测试入口不作为体验包分发。没有修改二维码地址。

## 用户追加授权后的产品与功能审计

当前轮范围：本地修复、回归、提交推送、NightFlix 测试分发。仅 47.110.245.29，不涉及闪创。没有后台业务源码修改，不需要重启四个后台服务。

| 问题 | 根因与处理 | 当前证据 |
| --- | --- | --- |
| 评论关闭红屏/巨量溢出 | 输入控制器在路由退出动画尚未结束时销毁；等待 ModalBottomSheetRoute.completed 再释放 | 修复前回归实际报 disposed TextEditingController 和 99630px overflow；修复后反复输入/关闭/重开、键盘 inset 的组件及 Android 回归通过。用户照片的 inactive-elements 断言未单独证明同源，不以关闭断言掩盖 |
| 评论没有时间 | 模型和 API 已有 createdAt，UI 没渲染 | 使用设备本地时间及 Material 本地化格式；真实本地 API 创建、重新读取、时间文本校验和删除通过 |
| 空评论点击无反馈 | 空内容按钮仍可点击后直接 return | 空白时禁用发送；增加发送提示及显式关闭按钮；键盘显示时约束内容高度 |
| 登录弹窗不可读 | 浅色 Theme 没有覆盖深色 Material 继承的白色 DefaultTextStyle | 浅色 Material 重建文字样式；Android 测得标题亮度从 1.0 降为 0.0；保持圆角裁切 |
| 剧场筛选无内容差异 | 线上分类表为空，90 部 scv2 测试剧 category_id 全空；接口过滤逻辑本身正常 | 模拟器真实 Nest/PG 两分类不同结果、恢复全部通过；补充受范围和版本保护的数据脚本。无分类时显示真实空态，不假造筛选结果 |
| 账户详情泄露技术字段 | 遍历 item.entries 直接展示接口字段 | 只显示类型、金额、余额、时间、权益有效期和记录编号；保留编号便于客服查账；有余额模拟器截图已核验 |

追剧刷新最初被误判：测试脚本查找 Following，但实际按钮文案为 Followed。正确脚本在撤掉额外监听改动后也通过，未保留多余刷新代码，也不计入修复数量。

本轮已完成：65 项 Flutter 单元/组件、877 项 API 测试（另 11 项 opt-in 未在通用命令执行）、8 项单独真实 PG 测试、8 项 Android 补充场景。测试数量不等于按钮数量；外部支付、广告、第三方登录和推送未配置，不计作真实商店/广告验收通过。设置完整回归、最后修改复测及发布校验仍待本轮后续记录。

分类操作：备份 `/var/backups/nightflix/before-categories-20260908.dump`，SHA256 `181e07118d231a4f0adba38e3cfcabd0ce584f117223a91b29682139028868fc`。首次保护检查只匹配到 81 条（前 9 个 code 带零），事务完整回滚，分类仍为 0；确认实际 code 后修正匹配并补充前导零的本地 PG 回归，不放宽租户、状态、90 条总数或版本校验。

第二次分类事务成功：90 部剧保留原上架状态、媒体和权益，仅设置分类并递增版本。爱情 19、古装 18、玄幻 20、科幻 4、剧情 29；公网接口和模拟器实际筛选、网格总数、首部标题及恢复 90 部全部验证通过。

最终 Android 复测 4 个场景通过（3 分 44 秒）：真实本地 API 的分类/点赞/收藏/追剧双向切换和持久化、评论发送/时间/举报落库/删除；评论输入关闭重开；登录对比度；真实远程目录、视频、选集、筛选、搜索和主要导航。完整设置 9 个业务场景重新通过（2 分 42 秒，额外 setup/teardown 不算业务场景）。加上有余额账户详情、账户页面及原生播放器/OCR检查，共 18 个不同 Android 业务场景；重复回归不累加。完整日志保存在 `outputs/android-product-fix-20260908/`。

外部服务边界仍然有效：Google/Apple 登录、AdMob、商店支付及推送服务未提供配置，本轮只验禁用/失败降级态；真实商店交易、广告填充和推送送达不能标记为通过。iOS 未参与本轮 Android 验收。远程测试剧未提供多语言媒体轨道，真实字幕/配音切换不属于本轮已有数据的通过证据。没有使用真实付费或向外部用户发送测试消息。
