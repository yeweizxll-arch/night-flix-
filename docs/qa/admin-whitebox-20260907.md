# 后台白盒与可用性审计（本地进行中）

## 范围与证据标准

- 工程：Night Flix；独立分支 `fix/admin-whitebox-20260907`，基于 `ea5d22f`。不改闪创工程，不部署任何服务器。
- 总部后台，以及代理商 A（星河短剧）、B（海风剧场）分别登录；使用独立临时 PostgreSQL 数据库、全量迁移和实际非超级用户 RLS 运行角色。
- 浏览器为内置浏览器，1280×720；图片、DOM、请求状态保存在本机 `outputs/admin-whitebox-20260907`。
- 证据分开计量：源码按钮绑定审查、组件 DOM 测试、API/数据库测试、真实浏览器操作。仅有事件处理函数不代表按钮已通过可用性测试。
- 外部支付、推送、邮件、DNS、商店签名、真实对象存储未接入时，不将禁用状态或模拟适配器通过写成外部服务成功。

## 已定位并修复

| 问题 | 修复 | 已有验证 |
| --- | --- | --- |
| 本地后台因共享合同包 CJS 导出而白屏 | Vite 显式预编译合同包 | 两后台实际进入；双构建 |
| App 运行配置直接暴露 JSON | 语言、地区、广告位、商店商品改为表单；保留非编辑字段 | 表单转换单测；浏览器保存及回读 |
| 普通运行账号保存 App 配置返回 500 | 为实际运行角色补两个数据校验函数的最小执行权限 | 原生 PG 复现及保存通过；授权集成测试 |
| 只读员工及仅有站点权限的员工看到错误入口或额外 403 | 按模块权限独立加载和禁用编辑 | 组件测试；只读账号 UI；API 200/403/404 |
| 未上架公共剧可点排行却返回 404 | 仅已上架/已下架内容显示排行入口 | B 租户复现；组件回归 |
| 拒绝内容使用浏览器 prompt；取消上架遗留价格 | 正式原因表单；取消清理表单 | 真实拒绝、再通过、上架、取消再打开 |
| 金额让运营输入最小货币单位；切币种残留上个币种金额 | 以元/美元等单位输入，精确转整数；按币种回填 | USD 9.99；三位小数拒绝；USD/JPY/EUR 组件测试 |
| 内容定价强制手输 UUID | 新增受租户/RLS/授权约束的内容搜索选择；列表显示剧名 | A 私有剧定价 60 金币；B/未授权内容不可选；API 集成测试 |
| 总部代理商选择要求手输 UUID | 分页搜索名称/代码，保留无查看权限的受控编号输入 | 财务按名称/代码筛选；授权按代理商名称选择 |
| 审计详情显示原始 JSON | 字段表格、中文值与密钥脱敏 | 组件脱敏与 XSS 测试 |
| 登录续期后业务失败会误退出 | 只在续期失败时清会话；保留重试幂等键 | 403/409/500 组件测试 |
| 退出请求失败仍显示已退出 | 服务确认后才清会话，失败保留重试入口 | AuthProvider 测试 |
| 新员工默认选中第一个角色，可能是所有者 | 角色必须显式选择；中文权限标签 | 真实只读员工创建；组件必填测试 |
| 手填 ISO 时间不适合运营操作 | 内容排期、法律生效、通知排期/人群时间使用日期选择器 | 法律发布与通知排期真实操作；日期转换测试 |
| 品牌图片只留资产编号输入 | 代理商可直接打开现有安全上传流程 | 上传弹窗与无存储状态；完整字节上传尚未本次浏览器验证 |
| 单文件上传错误过滤公共存储 | 与批量上传一致提供已启用公共存储；增加空配置和失败重试提示 | 组件测试；实际空配置弹窗 |
| 敏感词停用后无恢复按钮，租户可误看到公共词停用入口 | 本范围词可恢复，公共词只读 | 实际创建/停用/恢复；组件范围测试 |
| 治理/分销分页无记录仍计算出下一页 | 按已返回条数估算下一页 | 组件末页禁用测试 |
| 通知“编辑”只打开详情，需再点一次 | 加载后直接进入编辑；失败可重试 | 浏览器回填/保存；组件成功/失败测试 |
| 总部创建错误显示在弹窗背后 | 弹窗内提示；重复代码明确说明；取消清空 | 重复提交、关闭重开、成功创建真实操作 |
| 用户危险操作确认缺少具体对象 | 显示用户名与编号，保留原因和确认必填 | 停用、恢复、全部下线真实操作 |
| 页面出现开发要求/实现说明、英文错误 | 清理无关说明；保留资金、不可逆行为和缺配置提示；通用异常中文化 | 页面遍历；HTTP 错误单测；截图前后对照 |
| 提现后余额账本立即 500；分销冲正账本也存在同类问题 | 仅账本变动允许负数，余额/价格仍拒绝负数 | 财务集成、数值边界单测；真实 PG 与浏览器负数流水 |
| 账本仅按时间翻页，时间相同或微秒截断时漏行 | 按记录编号回查完整时间，以时间+编号游标分页，保留旧参数兼容 | 原生 PG 一条一页完整取回；财务集成与组件游标测试 |
| 总部处理提现后，代理商无刷新入口 | 同时刷新余额、流水、提现列表并清理旧提交提示 | 驳回后刷新：可用 90→100、冻结 10→0 |
| 重新上架不回填价格和地区限制 | 公共剧列表返回本租户原设置，弹窗回填；取消不污染其他剧 | A/B API 独立数据；构建制品回填 30 金币、US/SG、屏蔽 CN 并再次上架 |
| 内容分成仍显示最小单位、要求输入基点和 ISO 时间 | 以币种金额、百分比、日期选择器操作；BigInt 精确换算，保留财务校验 | USD/JPY/BHD、超精度/超范围单测；分成 100% 与私有剧 0% 创作者真实校验 |
| 快速切换分成代理商时旧请求可能覆盖新筛选结果 | 过期响应不写状态；加载期间清理旧基数状态并禁用月结 | 专项乱序组件回归：A 后返回不覆盖 B 金额，也不请求 A 的核算状态 |
| 核算状态读取失败后月结按钮仍可操作 | 未读取到有效核算状态时禁用月结 | 失败组件测试确认按钮禁用且不发送结算请求 |
| 员工密码弹窗与保留的创建弹窗输入编号重复，新密码没有可访问名称 | 后台每个 Form 使用独立名称前缀 | 全表单扫描；最终构建中三个密码输入分别关联正确标签；旧密码 401、新密码 200 |
| 反馈回复输入无明确标签，固定页数却允许切换页大小 | 输入标签与固定分页统一，时间按本机格式展示 | 失败保留输入、成功回显与只读组件测试；真实反馈回复/审计记录 |
| 停用存储只提示不能上传，未提示可能影响已有播放 | 明确说明上传与已有内容播放影响 | 真实创建、编辑、停用、启用、取消删除、确认删除 |

## 真实浏览器已执行

| 模块 | 已执行状态与操作 | 尚需补齐 |
| --- | --- | --- |
| 导航/登录 | HQ、A、B 登录；全部默认菜单进入；A 退出重登；只读菜单隔离；双作用域构建后再次登录 | 并非每个浏览器/视口组合 |
| 公共剧池 | 空拒绝原因阻止、拒绝、重新通过、取消上架后价格清空、上架；B 仍待审核；权重 50/置顶 1 保存回读；下架及重新上架 | 真实 App 展示不属于本次后台测试 |
| App 配置 | 无效分享域名阻止；语言选择、保存、重新读取；只读禁用 | 广告/商品所有增删和冲突 UI 状态 |
| 商品 | 新会员必填、创建、多语言、USD 9.99、停用取消/确认/恢复；B 无 A 商品；A 私有剧金币定价；100+10 金币包创建、USD 1.99 回读、编辑名称；私有剧第 1 集现金价 0.99→1.29、停用与恢复 | 单集金币价及所有币种/语言组合未逐组合真实点击；组件测试覆盖多币种转换与回填 |
| 权限 | 新只读角色与员工；只读登录；API 越权 403、跨租户内容 404；HQ 角色创建、保存权限；HQ 临时员工创建、停用、恢复；密码不一致/确认必填、重置成功后旧密码 401/新密码 200；撤销会话原因/确认必填及提交 201；额外原生 HTTP 验证旧令牌从 200→401 | 不涉及外部身份服务 |
| 法律 | 新建、编辑、展开、发布时间必填、发布后编辑/删除隐藏；新草稿删除确认及列表消失；B 测试用户请求擦除 202 后后台有数据列表与详情：已提交、擦除未执行 | 本地浏览器未运行擦除 Worker 至完成状态；既有 API 集成覆盖处理流程 |
| 通知 | 新草稿必填、创建、一次编辑、语言增加/移除、保存、排期必填/成功、取消必填/成功；B 不可见 | 真实外部推送不在本地成功结论内 |
| 社区 | 新敏感词必填、创建、停用、恢复；评论通过/隐藏/删除/恢复；A 举报解决、B 举报驳回；反馈回复；A/B 独立记录；B 弹幕实际创建后隐藏、通过、删除、恢复为隐藏，必填原因校验 | 第三方推送与通知送达不在本地结论内 |
| 用户 | 详情；停用原因/确认校验、停用、恢复、全部下线；掩码显示；单设备撤销提交成功 | 单设备涉及真实推送令牌的外部失效未测 |
| 总部代理商 | 新建必填、重复代码、取消重开清空、成功创建；A 品牌改名保存后 A 回填 | 构建/域名各状态与搜索回归 |
| 存储/品牌 | 上传入口、未配置存储时不能上传；A 品牌配置读取；本地假凭据存储创建/编辑/停用/启用/删除 | 完整文件字节上传、真实 S3/服务器存储连接未本次浏览器验证 |
| 内容分类与版本 | 代理商分类创建必填/创建/编辑/删除/恢复；HQ 已发布公共剧创建新版本，原版保持发布、新版为草稿 | 所有导入导出、剧集轨道按钮仍需完整有数据浏览器流程 |
| 财务 | 提现必填/提交/撤回、总部驳回和批准；余额冻结/解冻/负流水；按代理商名称和代码筛选；T+3 策略保存；本地到期结算 | 真正打款及凭证核验未执行 |
| 分销/分成 | 一级分销 5% 规则保存；三方比例总和校验、保存、编辑；私有剧非零创作者拒绝，20/80/0 保存 | 商店净收入凭据、内容广告对账、历史报告、月结的浏览器全链路尚未覆盖 |
| 支付/订单/授权 | 默认页与空态；Stripe 必填及 LIVE 警示；授权包必填/创建/选剧保存（版本 0→1）；单剧授权日期必填及发放 201、撤销原因必填/撤销后只读；现有 API 集成覆盖状态和越权 | 外部真实连接与退款；授权包整体发放、到期定时边界未本次真实浏览器验证 |

## 测试运行记录

- API 最终全量：131 文件 / 873 通过，3 文件 / 9 跳过；日志 `/tmp/nightflix-api-final-test3.log`，包含最终游标与公共剧回填修复。不要把专项重复用例简单相加。
- 原生 PostgreSQL 专项：2 文件 / 14 通过；日志 `/tmp/nightflix-native-final-test.log`。浏览器临时服务器额外使用原生 PG 非特权运行角色，最新游标、负数、公共池 A/B 隔离 HTTP 核验通过。
- 前端最终全量：25 文件 / 111 通过；日志 `/tmp/nightflix-admin-final-test10.log`，包含表单名称扫描、分成乱序和核算读取失败保护。
- API/后台类型检查、API 构建及总部/代理商独立构建均已通过。最终后台制品日志 `/tmp/nightflix-platform-build-final4.log`、`/tmp/nightflix-tenant-build-final4.log`；已实际打开最终双构建并核对密码标签和运行配置页面。
- 最终运行配置截图 `13-final-built-runtime.png` 与同视口原始 `02-before-runtime.png` 对照：移除原始 JSON 编辑器和开发要求说明，保留选择器、表单和必要的输入格式提示。
- 中间失败均有记录：反馈成功后模态框保留的隐藏文本造成重复匹配，断言改为可见表格单元格；50 行财务 DOM 测试在 30 秒超时，缩小查询范围并给该大表用例 60 秒上限后通过。没有移除业务断言。
- 已输出 `admin-control-inventory-20260907.md`：显式按钮定义逐项绑定清单，**静态绑定不是功能验收**，也不包含组件自动生成的全部控件。
- 本报告进行中，**尚不宣称每一个按钮及全部状态都已验收**。本地实际执行、组件测试、API 测试与外部服务未验证项目分开记录。

## 本地复测入口与后续范围

- 总部构建预览：`http://admin.localhost:4544`；代理商 A/B：`http://tenant-a.localhost:4545`、`http://tenant-b.localhost:4545`。
- 预览依赖显式启动的临时 HTTP/PG 测试夹具，不是线上地址，也不是长期服务。测试账号 `qa-admin` / `qa-owner`，密码仅限该夹具：`Local-admin-test-123`。不适用于真实环境。
- 夹具源码 `apps/api/test/admin-browser.integration.spec.ts`；仅当 `NIGHTFLIX_ADMIN_BROWSER=1` 且数据库为本机专用 `nightflix_local_features` 时启动；绑定 127.0.0.1，并在显式完成后删除本次独有的临时库及角色。
- 剩余浏览器重点：真实文件字节上传与导入/导出、剧集轨道、授权包发放与到期边界、财务凭据登记/月结全链路、域名与构建任务各状态。不能拿现有 API 集成通过替代这些按钮的浏览器全状态验收。
- 本轮只在本地修复、测试与生成构建制品；没有部署、数据库线上变更、APK 或二维码更新。
