# 代理商独立运营：追加白盒与可用性验收

## 范围

- 延续 `fix/admin-whitebox-20260907`，基于本地提交 `7e13fce`；只改 Night Flix。没有改闪创、线上数据库、APK、二维码或真实第三方配置。
- 总部负责公共内容、SaaS 技术接入、授权、约定的分成对账；代理商负责自己的品牌、用户及私有内容运营。总部不能代代理商改品牌、停用用户或强制用户下线。
- 保留总部技术停服与域名/证书维护，不把它与代理商运营开关混为一谈。
- 旧 `platform_collect` 退款与代理商 `tenant_direct` 退款已存在服务端归属检查。本轮未改历史代收退款政策；有关历史代收是否保留总部退款的询问尚无答复，不影响其他修复。
- 沿用 Product Design 审计的先截图再操作方式，使用现有 Ant Design 页面，不重新设计主题。卡帕西与 Ponytail 准则用于限制改动范围，复用现有事务、上传、幂等、审计及租户隔离机制。

## 已修复问题

| 编号 | 问题与根因 | 修改与验证 |
| --- | --- | --- |
| I01 | 总部旧用户管理权限仍可停用代理商用户、撤销会话 | 共享业务服务在事务前拒绝总部写操作；HQ 页面只读；权限目录与角色回填排除废弃经营权限。旧令牌仍被服务端拒绝。实际 HTTP 403，代理商原功能保留，跨租户查询 404。 |
| I02 | 总部品牌修改接口与表单参与代理商运营 | 接口 403；总部表单禁用，代理商自行保存并回读；B 品牌不受 A 影响。技术开关明确为“SaaS 服务接入”。 |
| I03 | 代理商没有私有剧字幕/配音维护入口 | 新增受租户权限与 RLS 约束的保存/停用接口，接入现有上传弹窗。支持默认轨、编辑、停用、恢复；已上架只读，先下架再改。不允许写公共剧、外租户剧或外租户媒体。 |
| I04 | 轨道缺少数据库租户写策略；媒体被隔离后无法停用轨道 | 迁移 0047 只开放本租户私有剧轨道 INSERT/UPDATE；纯停用可解除不可用媒体，重新激活仍要求可用文件。测试隔离媒体停用成功、恢复及直接数据库激活失败。原生 PG 非特权 RLS 测试通过。 |
| I05 | HQ 停用轨道未核对 URL 中的剧与集归属 | 停用前验证剧集属于已锁定的可编辑公共剧。错误父剧 404，原轨道不变，防止借别的草稿修改已锁定作品。 |
| I06 | 已下架私有剧的界面仍称只读 | 文案与真实可编辑状态一致；浏览器实际下架、添加字幕、添加配音、停用取消/确认、恢复并回读。 |
| I07 | 不满足导出条件的 409 被说成版本冲突，提示很快消失 | 明确 `CONTENT_EXPORT_NOT_READY` 与可处理的缺封面、媒体不可用、规模超限原因；导出栏保留失败提示。未放宽导出/重新导入约束。 |
| I08 | 导入任务详情显示 JSON 摘要、英文状态及技术术语 | 改为成功部数/失败行数、已导入/校验失败和中文失败原因，保留排障任务编号；不显示数据库错误代码。浏览器成功导入报告已检查。 |
| I09 | 注销中/已注销账号显示“停用”，且提供“启用” | 补全状态类型、筛选和标签，隐藏无意义写按钮；服务端提前拒绝状态恢复。实际请求→Worker 擦除→后台完成、旧账号登录 401；注销不可逆约束保留。 |
| I10 | 隐私详情暴露内部保留类别与原因代码 | 中文显示交易财务、佣金、安全审计及保留原因；第三方处理提示按服务类别说明，不改变任何保留期限或策略。 |
| I11 | TLS 处理中仍能点主域名切换，随后泛化 400；域名页无手动刷新 | TLS 生效前显示明确说明且无切换按钮；新增刷新。DNS 不匹配返回中文指引并保留页内错误。已执行停用取消/确认、启用、证书状态必填、模拟证书生效、主域名取消/确认。 |
| I12 | HQ 用户查询已选租户仍显示大块选择提示，操作栏被挤出屏幕 | 已选时移除提示；操作列固定右侧；日期用原生日期控件。1280×720 同视口截图对比，详情入口可见。 |
| I13 | HQ 排行查询仍要求手输代理商 UUID；构建页误称 H5 域名 | 复用名称/代码选择器；构建页显示“App 服务域名”“内部测试”。核对实际构建器为 Flutter，接口旧字段名保持兼容。 |

## 本次实际操作证据

证据目录：`/Users/yewei/Documents/Codex/2026-08-31/zhe/outputs/admin-independent-ops-20260907`。

1. **总部/两个代理商隔离，已验证**：真实登录、HQ 旧权限写操作 403、B 读取 A 用户/私有剧/导入任务 404、A 使用 B 媒体 400 且版本和轨道不变。`scripts/qa-admin-independent-ops.mjs` 使用仅本机临时库，输出 `13-independent-http.json`。
2. **私有字幕配音操作，已验证**：空表单校验、VTT 保存、默认设置、停止取消/确认、恢复、MP3 配音、关闭重开回读；截图 `03-tenant-track-saved.png`。真实字节上传尚未算入此项。
3. **导出/导入处理，分层验证**：API JSON/CSV 均 200；导出内容更换编号后实际入队、真实导入 Worker 完成，B 不可读。浏览器双导出点击成功、任务详情与刷新均成功，截图 `07-import-report.png`。未把浏览器下载写盘/文件选择器导入算成已验收。
4. **账号注销全流程，已验证**：`scripts/qa-admin-privacy.mjs` 在临时库请求擦除并运行真实 Worker，完成标记与登录拒绝已断言；浏览器 B 列表/详情确认“已完成、本地擦除已执行”，用户栏“已注销、只读”。`08-privacy-completed.png`、`09-erased-read-only.png`。
5. **总部不经营，已验证**：总部品牌禁用、A 自行改品牌；`10-hq-brand-read-only.png`。`04-hq-read-only.png` 与 `12-hq-customer-final.png` 同视口对比，移除无关大提示，右侧只剩详情与只读。
6. **域名流转，受控验证**：本地 DNS 适配器仅对 `approved.qa.example.test` 返回成功；其余失败。使用显式 `qa-fixture/no-real-certificate` 工单模拟证书状态，验证 UI 状态和服务端规则；**未签发证书或修改真实 DNS**。截图 `11-domain-state-flow.png`。最终制品再次点击失败与“重试”，两次真实 POST verify 均返回 400，错误保持可见；不是只刷新列表。见 `17-latest-api-state.json`、`18-dns-retry-final.png`。
7. **构建入口，部分验证**：缺图标/域名/应用编号时必填与禁用状态正确；已核对底层是 Flutter。没有用虚构构建任务宣称 APK/IPA 成功。

## 测试记录

- API 全量：131 文件、877 项通过；10 项可选测试跳过，见 `/tmp/nightflix-independent-api-final2.log`。后续 DNS 中文失败路径专项 6 项通过，见 `/tmp/nightflix-independent-dns-api.log`。
- 最终轨道隔离媒体停用/恢复与迁移专项分别 5、7 项通过：`/tmp/nightflix-independent-track-migration-final.log`、`/tmp/nightflix-independent-migrations-final.log`。迁移包括重复执行检查。
- 原生 PostgreSQL 最终专项 7 项通过，见 `/tmp/nightflix-independent-native-final.log`，覆盖真实非特权 RLS。
- 隐私/用户管理专项 13 项通过，见 `/tmp/nightflix-independent-privacy.log`。
- 前端最终完整回归 **25 文件 / 119 项全部通过**：`/tmp/nightflix-independent-admin-final5.log`；其后 DNS 重试分支专项通过 `/tmp/nightflix-independent-dns-ui-final.log`。不累加专项重复用例作为通过总数。
- 前端中间失败有记录：一次财务测试在 Ant Design 按钮加载过渡期间用文本定位提前点击；改全页 role 查询后，50 行表格的 jsdom 可访问性计算又超时。最终改为先按可见文本定位按钮、明确等待按钮结束 loading，再点击；资金断言及分页条件未放宽。该专项 11.17 秒通过，随后全量通过。
- 最终 API/后台类型检查与 API、HQ/代理商独立构建通过；日志 `nightflix-independent-api-build3.log`、`nightflix-independent-hq-build7.log`、`nightflix-independent-tenant-build7.log`。构建包较大及 Ant Design 弃用提示是已知非阻断警告，不等同所有易用性问题消失。
- 最后一次临时数据库重建后，两组 HTTP 脚本再次通过：`19-independent-http-latest.json`、`20-privacy-http-latest.json`。旧临时库及其专属角色由夹具清理，仅测试数据被删除，证据文件保留；没有删除用户或线上业务数据。

## 尚未完成的验收，不能冒充通过

- **本地可补的剩余流程**：文件选择器到完整字节上传/下载落盘、财务凭据→核算→月结完整浏览器链路、授权包整体发放/到期边界、App 广告/商店商品配置所有新增删除及冲突状态。
- **需要完整依赖环境的流程**：真实对象存储连接、大文件/失败续传、构建 Worker 到实际产物下载、商店签名/真机包、外部 DNS/TLS、真实打款和第三方送达。
- 上述区别是“未执行完的本地操作”与“外部环境验证”，不是把所有剩余项目都归咎于用户没提供配置；也不是所有按钮已验收。
- 本地修复尚未部署。本文范围不授权总部参与代理商内容审核、营销或用户运营。
