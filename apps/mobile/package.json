{
  "name": "@drama/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "android:debug": "pnpm prepare:web && cap sync android && cd android && ./gradlew assembleDebug",
    "build": "pnpm prepare:web && cap sync",
    "ios:simulator": "pnpm prepare:web && cap sync ios && xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Debug -sdk iphonesimulator -derivedDataPath ios/DerivedData CODE_SIGNING_ALLOWED=NO build",
    "prepare:web": "VITE_H5_RELEASE_CHANNEL=test pnpm --filter @drama/h5 build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/ios": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
