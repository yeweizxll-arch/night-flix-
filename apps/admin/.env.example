# Build the platform administration site by default.
# Allowed values: platform | tenant
VITE_ADMIN_SCOPE=platform

# Optional local-development switch. Keep false or unset for production builds.
# The UI also requires import.meta.env.PROD=false before showing Fake payment tools.
VITE_ENABLE_FAKE_PAYMENT=false
