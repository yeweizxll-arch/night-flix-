# 批量剧集上传与自动时长

适用于 Night Flix 总部公共内容管理和代理商内容管理，不涉及闪创项目。

## 使用

1. 创建短剧草稿，打开详情，选择“批量添加剧集”。
2. 一次选择多个 MP4 / MOV / WebM。系统按文件名自然排序，从现有最大集数后连续编号；
   上传前可修改起始集数、每集编号和标题，标题默认取文件名。
3. 每个文件单独读取视频时长，向上取整到秒。不再默认 60 秒或要求手填；
   单集上传也自动填写只读时长。无法读取的文件会明确报错，不使用虚构时长。
4. 点击“上传并添加 / 重试未完成”。按集串行安全上传与保存，已成功的集数不会重复上传；
   部分失败可重试。已有同号剧集不覆盖，需调整编号。
5. 完成后返回短剧详情。批量操作只创建正片草稿，不自动发布、不生成试看视频。

浏览器读取本地文件 metadata，不是服务器 ffprobe 或自动转码。旧记录不会被自动改写。
每集最大 2 GiB，每部最多 1000 集；浏览器必须支持所选视频格式。
保持页面打开，可在当前窗口暂停/继续；关闭或刷新不会保留未完成文件队列，已保存剧集保留。
文件继续存到已启用的服务器硬盘存储，沿用租户隔离、SHA256、临时签名、完成校验及内容版本 CAS。

## 可重复验收

- `pnpm --filter @drama/contracts build`
- `pnpm --filter @drama/admin test`：56 项，包括 8 项批量/自动时长回归。
- `pnpm --filter @drama/admin typecheck`
- `smoke-batch-episodes.mjs` 使用独立无头 Chrome、真实测试 API、真实 HTTPS/S3 及自制 MP4，
  检查自然排序、3/7/11 秒逐集保存、单集 7 秒自动填写、失败文件重试不重传成功文件。
  默认只在该测试浏览器内替换静态页面为本地制品；`--live` 则直接访问已部署页面。
  每次运行会创建独立、未上架测试草稿，不能误当无写入检查；凭证仅从私有 JSON 读取。

```sh
PLAYWRIGHT_MODULE=/absolute/playwright/index.mjs node deploy/test-server/smoke-batch-episodes.mjs \
  /absolute/artifact /private/access.private.json /private/synthetic-mp4-directory
```

测试视频目录包含 `EP_01.mp4`（3 秒）、`EP_02.mp4`（7 秒）、`EP_10.mp4`（11 秒）。
测试脚本不发送邮件、不调用付费服务、不发布短剧、不修改用户已有短剧。

应用改动无数据库迁移、无新增服务或依赖，不需要重装 APK。

## 2026-09-07 部署与实测

- 运行 commit：`1ed89406fff03a5fbcd8dfc57ae7f59d2f66e282`。
  当前链接：`/opt/nightflix/releases/1ed8940`。
  本地制品 SHA256：`e0d7843a75e622ec6e16f22c34dc0fe6c6f54afdaf2f7339bfe707eaffbba164`。
  服务器仅接收并校验制品、解压、启动隔离候选和切换；没有构建或安装项目依赖。
- 本地后台 56 项测试、类型检查通过；API、两个后台和 H5 构建通过。
- 真实 Chrome 验收先发现批量选择框清空时，AntD 回填文件路径导致页面异常；
  已改为原生 file input 后重新构建与验证。初始 `53fe367` 候选没有切换，不可作为回滚目标。
- 两个后台均实测一次多选三个视频、自然排序、自动读取 3/7/11 秒、真实签名上传、
  完成校验、保存三个剧集，再单独上传第 4 集并自动保存 7 秒。无页面运行时错误。
  代理商流程额外注入第二个 PUT 返回 503，首次成功 2 集，重试只上传失败文件；
  总计 4 次 PUT 完成 3 集，没有重传成功文件。
- 完整验收草稿：代理商 `01a07782-9c67-751e-99c3-d8f8688719b0`、
  总部 `01a07788-0b29-7af4-acd3-e8b2eeddd790`。均为自制合成视频、未上架，
  每部 4 集、时长 `[3, 7, 11, 7]`、试看秒数均为 0。
  另有两个中间验收草稿，各 3 集；三个空脚本草稿已软删除，可在保留期恢复。
  用户已有 `001` 短剧和素材没有修改。
- 切换后公网 HTTPS 27 项冒烟通过，包括三个入口健康、登录、Cookie、Host/Origin、
  跨服务/跨身份拒绝、游客配置和用户钱包。分别核对两个后台实际返回的 HTML、JS、CSS
  SHA256 与上述浏览器测试制品一致；服务重启后两部验收草稿的四集及时长仍正确。
- 备份：`/var/backups/nightflix/nightflix-before-batch-episodes-20260906.dump`，SHA256
  `09edfe7e36b545c4ea9e06c2ce1d3b1ae58263097eeac085d571a168bcfcf569`。
  回滚点为 `/opt/nightflix/releases/c0e2ca1`；回滚应用只切链接和重启四个服务，
  不覆盖数据库、不删除存储目录或新上传的视频。本次无数据库结构变更。

浏览器完整上传验收使用本地候选静态资源及真实测试 API/S3；切换后的验收为公网接口、
静态资源逐字节一致性及持久化回读，不冒充再次在部署后全量重跑浏览器或手机播放验收。
